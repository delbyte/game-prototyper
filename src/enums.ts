export enum BiomeMaterialType {
    STANDARD = 'standard',       // Normal terrain (grass, dirt, rock, stone, snow)
    CRYSTAL = 'crystal',         // Glass, crystal, transparent materials
    GLOWING = 'glowing',         // Lava, magma, fire, emissive materials
    WATER = 'water'              // Rivers, lakes, oceans
}
